[project]
name = "keyboard-next-word-prediction"
version = "0.1.0"
description = "A small next-word prediction project for keyboard suggestions"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=5.0.1",
]
